# text speed

The speed at which text should be printed using the [animate at speed](./animate-at-speed) block. Can be modified by using [tags](./create) in the text of a TextSprite.

```sig
fancyText.__speedPicker(fancyText.TextSpeed.Normal)
```

```package
arcade-fancy-text=github:riknoll/arcade-fancy-text
```